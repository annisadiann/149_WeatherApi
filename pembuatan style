<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Creative Weather Display</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
         body {
            font-family: 'Poppins', sans-serif;
            background-color: #f0f2f5;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        .search-container {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            width: 100%;
            max-width: 350px;
        }

        .search-container input {
            flex-grow: 1;
            padding: 10px 15px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 1em;
            font-family: 'Poppins', sans-serif;
        }

        .search-container button {
            padding: 10px 20px;
            border: none;
            background-color: #2193b0;
            color: white;
            border-radius: 8px;
            font-size: 1em;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .search-container button:hover {
            background-color: #1c7a92;
        }

        .weather-card {
            background: linear-gradient(135deg, #6dd5ed, #2193b0);
            color: white;
            padding: 25px;
            border-radius: 15px;
            width: 100%;
            max-width: 350px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
            box-sizing: border-box;
            text-align: center;
        }
        .weather-card .location .city { margin: 0; font-size: 2em; font-weight: 600; }
        .weather-card .location .region-country { margin: 5px 0 15px 0; font-size: 1em; opacity: 0.9; }
        .weather-card .main-info { display: flex; align-items: center; justify-content: center; margin-bottom: 15px; }
        .weather-card .main-info img { width: 70px; height: 70px; margin-right: 15px; }
        .weather-card .main-info .temp { font-size: 4em; font-weight: 700; }
        .weather-card .condition { font-size: 1.2em; text-transform: capitalize; margin-bottom: 20px; font-weight: 300; }
        .weather-card .details { display: flex; justify-content: space-around; background: rgba(255, 255, 255, 0.2); padding: 10px; border-radius: 10px; font-size: 0.9em; }
        .weather-card .details div b { font-weight: 600; opacity: 0.8; }
    </style>
